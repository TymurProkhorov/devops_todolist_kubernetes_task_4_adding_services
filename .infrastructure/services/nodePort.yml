apiVersion: v1
kind: Service
metadata:
  name: nodeport
  namespace: todoapp
spec:
  selector:
    app: todolist
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30008
  type: NodePort